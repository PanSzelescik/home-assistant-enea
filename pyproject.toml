[project]
name = "home-assistant-enea"
version = "1.4.3"
requires-python = ">=3.14.2,<3.15"

[dependency-groups]
dev = [
    "homeassistant>=2026.3.3",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
